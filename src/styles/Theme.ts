// src/styles/theme.ts
import breakpoints, {
  device,
  spacing,
  fontSize,
  radius,
  sizes,
} from "./Breakpoints";

const theme = {
  breakpoints,
  device,
  spacing,
  fontSize,
  radius,
  sizes,
};

export default theme;
